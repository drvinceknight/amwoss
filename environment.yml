name: ampwoss
channels:
  - defaults
dependencies:
  - numpy
  - pandas
  - python
  - pip
  - scipy
  - sympy
  - r-desolve
  - r-essentials
  - r-lintr
  - r-pracma
  - r-rootSolve
  - pip:
      - black
      - ciw
      - docformatter
      - dwys
      - invoke
      - nashpy
      - pulp
